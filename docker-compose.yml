version: '3'

services:
  strapi:
    alpha_test_strapi:latest
    build: .
    ports:
      - '1337:1337'
    environment:
      DATABASE_CLIENT: sqlite
      DATABASE_FILENAME: ./data/data.db
    volumes:
      - .:/usr/src/app
      - ./data:/usr/src/app/data

